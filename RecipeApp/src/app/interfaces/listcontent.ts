export interface Listcontent {
    id: string,
    userlists_id: string,
    recipeId: string,
    recipeLabel: string,
    recipeIngredientLines: string,
    recipeTotalTime: number, 
    recipeHealthLabels: string,
    recipeco2Emissions: string,
    created_at: string
}
